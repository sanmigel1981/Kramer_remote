name: Build APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install system dependencies
        run: |
          sudo apt update
          sudo apt install -y git zip unzip openjdk-17-jdk autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev cmake libffi-dev libssl-dev libltdl-dev
      
      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install cython==0.29.37
          pip install buildozer
      
      - name: Setup NDK 25b with symlink and remove broken files
        run: |
          # Скачиваем NDK 25b
          wget https://dl.google.com/android/repository/android-ndk-r25b-linux.zip
          unzip -q android-ndk-r25b-linux.zip
          # Удаляем проблемную папку с Python 2 файлами
          rm -rf android-ndk-r25b/sources/third_party/googletest/scripts
          # Удаляем старый NDK, если есть
          rm -rf /home/runner/.buildozer/android/platform/android-ndk-r23b
          # Создаём символическую ссылку
          mkdir -p /home/runner/.buildozer/android/platform/
          ln -sf $PWD/android-ndk-r25b /home/runner/.buildozer/android/platform/android-ndk-r23b
          # Устанавливаем переменную окружения
          echo "ANDROIDNDK=/home/runner/.buildozer/android/platform/android-ndk-r23b" >> $GITHUB_ENV
      
      - name: Build APK with Buildozer
        run: |
          buildozer android debug
      
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: kramer-remote-apk
          path: bin/*.apk